export type TErrorSouce = {
    path: string | number | (string | number)[];
    message: string;
  }[];

  export type TErorDetails = [] | null | number |string
  export type TErrorMessage = (string | number)[][]